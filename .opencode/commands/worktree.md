---
description: Create a git worktree in .worktrees/
---

Run the following command in the terminal:

!`git worktree add .worktrees/$ARGUMENTS`

Then list all worktrees with `git worktree list` to confirm the new one was created.
